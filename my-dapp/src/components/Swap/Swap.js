import React from 'react';

function Swap() {
    return (
        <div className="swap">
            <h2>Swap</h2>
            <p>This is where users can swap tokens.</p>
            {/* Add form or swap functionality here */}
        </div>
    );
}

export default Swap;
