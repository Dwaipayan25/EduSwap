import React from 'react';

function Pool() {
    return (
        <div className="pool">
            <h2>Liquidity Pool</h2>
            <p>This is where users can add/remove liquidity to/from pools.</p>
            {/* Add pool management functionality here */}
        </div>
    );
}

export default Pool;
